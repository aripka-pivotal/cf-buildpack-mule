#!/usr/bin/env bash
# bin/release <build-dir>
cat << EOF
addons: 
config_vars:
default_process_types:
  web: MULE_HOME=$PWD/local/mule-standalone-3.5.0 JAVA_HOME=$PWD/local/jdk1.7.0_67 PATH=$PWD/local/jdk1.7.0_67/bin:$PWD/local/mule-standalone-3.5.0/bin:$PATH mule -M-Dhttp.port=\$PORT
EOF
